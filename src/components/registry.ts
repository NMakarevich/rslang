const registry = {
  render: (): string => {
    return `
    <div class='main__registry'>
      <h2 сlass='main__title'>Регистрация</h2>
    </div>
    `;
  },
};

export default registry;
